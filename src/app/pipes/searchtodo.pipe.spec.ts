import { SearchtodoPipe } from './searchtodo.pipe';

describe('SearchtodoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtodoPipe();
    expect(pipe).toBeTruthy();
  });
});
