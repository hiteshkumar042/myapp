import { HighlighttaskDirective } from './highlighttask.directive';

describe('HighlighttaskDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighttaskDirective();
    expect(directive).toBeTruthy();
  });
});
